# Task Spec Template

**Title:** (e.g., Add overtime rules for 6-day week)

**Business Context (plain English):**
Why we need this, how it should work in Greece (EFKA, ΣΣΕ, Digital Work Card). No code.

**Acceptance Criteria (must have to consider "done"):**
- [ ] What inputs/outputs?
- [ ] Which roles/rates/rules?
- [ ] Error handling?
- [ ] Tests pass?

**Existing Files to Respect:**
List critical files to keep style/structure (e.g., `server/payroll_engine.ts`, `db/schema.sql`).

**Files Allowed to Change/Create:**
Name them if you know (or write "agent decides").

**Data/Rules (if any):**
Paste specific rules/tables/snippets (Greek/English OK).

**DB Changes (if any):**
Columns, types, defaults, migration notes.

**Non-Goals:**
What NOT to touch.