# Test Architecture Guidance Integration

Create a simple Greek payroll validation utility to test that the architecture guide is working.

## Requirements:
- Add a validation function in server/services/PayrollValidationService.ts
- Follow the architecture guide rules for Greek payroll compliance
- Keep the function pure and testable
- Add proper TypeScript types

This task should demonstrate that the AI understands the system boundaries and Greek payroll domain.