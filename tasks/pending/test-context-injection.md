# Test Architectural Context Injection

Create a simple function that demonstrates the agent now has full architectural context.

## Requirements:
- Add a function in server/services/ContextTestService.ts that validates EFKA compliance rules
- The function should follow the architecture guide patterns
- Use proper TypeScript types from the schema
- Include Greek payroll domain knowledge

This task should demonstrate that the AI agent now has complete architectural context.