Here’s a tight post-deploy acceptance + hardening checklist you can run right now against:

https://hr-master-katerina43.replit.app

1) Smoke checks (copy-paste)
# Basic reachability
curl -sv https://hr-master-katerina43.replit.app/ | head -n1
curl -sv https://hr-master-katerina43.replit.app/health | jq .
curl -sv https://hr-master-katerina43.replit.app/ready  | jq .

# Auth entrypoint should 302 to IdP
curl -svI https://hr-master-katerina43.replit.app/api/login | sed -n 's/^Location: //p'

# Whoami contract (unauthenticated)
curl -s https://hr-master-katerina43.replit.app/api/auth/user | jq .

# Rate limit on login (6th = 429)
for i in {1..6}; do curl -s -o /dev/null -w "%{http_code}\n" https://hr-master-katerina43.replit.app/api/login; done

# SPA fallback works for dashboard
curl -svI https://hr-master-katerina43.replit.app/dashboard | head -n1


Expected:

/health & /ready → 200

/api/login → 302 with Location: https://replit.com/oidc/auth?...

/api/auth/user → { authenticated: false, user: null }

6th /api/login call → 429

/dashboard → 200 (SPA) or 302 (your chosen redirect)

2) Session cookie sanity

After a real login round-trip (browser), confirm Secure + SameSite=None + __Host-psid:

curl -I https://hr-master-katerina43.replit.app/oauth2callback | grep -i '^set-cookie:'

3) Security headers quick pass
curl -sI https://hr-master-katerina43.replit.app/ | grep -Ei 'strict-transport|content-security|x-frame|x-content-type|referrer-policy'


If missing, add Helmet (CSP, noSniff, frameguard, referrerPolicy).

4) Observability pulse
# Auth health/metrics if exposed
curl -s https://hr-master-katerina43.replit.app/api/auth/health  | jq .
curl -s https://hr-master-katerina43.replit.app/api/auth/metrics | jq .

# Integrations status (MyDATA/ERGANI placeholders)
curl -s https://hr-master-katerina43.replit.app/api/integrations/status | jq .

5) Database schema sanity (optional, if you have psql)
psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM log_entries;"
psql "$DATABASE_URL" -c "SELECT column_name FROM information_schema.columns WHERE table_name='audit_events';"
psql "$DATABASE_URL" -c "SELECT column_name FROM information_schema.columns WHERE table_name='dr_slas';"
psql "$DATABASE_URL" -c "SELECT column_name FROM information_schema.columns WHERE table_name='oncall_teams';"

6) Final hardening to schedule next

Helmet + CSP in server (block clickjacking/XSS, set Referrer-Policy: no-referrer).

Soft-fail logging/audit (done) + keep STRICT_AUDIT=false outside prod.

Idempotent initializers to stop duplicate “initialized” logs.

Backups & rotation: nightly DB backup; rotate SESSION_SECRET quarterly.

Synthetic monitor: check GET /ready every minute with alert on !=200.

MyDATA & ERGANI: wire credentials when ready; keep warnings non-blocking till then.