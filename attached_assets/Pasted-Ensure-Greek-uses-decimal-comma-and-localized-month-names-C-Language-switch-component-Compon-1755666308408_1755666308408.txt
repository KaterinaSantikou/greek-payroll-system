Ensure Greek uses decimal comma (,) and localized month names.

C) Language switch component

Component updates i18n, formats, date-lib locale, and persists:

i18n.changeLanguage("el"), set dayjs/date-fns setDefaultOptions({ locale: el }).

Broadcast event langChanged for charts/third-party widgets.

D) Replace hard-coded strings

Inventory strings (grep for quotes in src/**). Replace with t("namespace.key").

Examples (nav):

nav.dashboard: "Dashboard" / "Πίνακας"

nav.people: "People" / "Προσωπικό"

nav.employees: "Employees" / "Εργαζόμενοι"

nav.onboarding: "Onboarding" / "Ένταξη"

nav.exits: "Exits" / "Αποχωρήσεις"

nav.time: "Time & Attendance" / "Χρόνος & Παρουσία"

nav.analytics: "Analytics" / "Αναλύσεις"

nav.settings: "Settings" / "Ρυθμίσεις"

Dashboard chips:

dashboard.digital_card_sync: "Digital Card" / "Ψηφιακή Κάρτα"

dashboard.ergani_sync: "ERGANI in sync" / "ΕΡΓΑΝΗ σε συγχρονισμό"

dashboard.filings_due_1: ICU plural

EN: "{{count}} filing due"

EL: "{{count}} δήλωση σε εκκρεμότητα", plural: "{{count}} δηλώσεις σε εκκρεμότητα"

E) ICU plurals & tokens

Use ICU for Greek pluralization:

time.hours: "{count, plural, one {ώρα} other {ώρες}}"

Avoid string concatenation; pass variables: t("hours_this_week", { count: 38.5 }).

F) Dates & relative time

Ensure relative labels (e.g., “Updated just now”) localize:

EN: "Updated just now"

EL: "Μόλις ενημερώθηκε"

Wire dayjs/date-fns relativeTime with el locale.

G) Backend messages & errors

Prefer error codes → map to localized strings on FE (errors.*).

If BE sends human strings, add a field locale_key for FE lookup.

H) Persistence & defaults

Language source of truth order:

User profile preferred_locale

Cookie lang

localStorage.lang

Browser navigator.language

Fallback en-US

Ensure SSR/CSR agree (if SSR app): preload resources by accept-language or cookie.

I) QA hooks & linting

Enable i18n lint: fail CI on missing keys or unused keys.

In dev, show a console warn for missing translations (do not fall back silently).