Phase 2 — Does the server actually have the callback routes? (and in the right order)
RoutesDump:
1) Dump all Express routes (method + path + file:line):
   node -e "const app=require('./dist/index.js').app; const list=(r)=>r.route && console.log(Object.keys(r.route.methods), r.route.path); app._router.stack.forEach(list);"
   # If not possible, add a temporary /debug/routes route that logs app._router.stack and call it:
   # curl -s https://<REPLIT_DOMAIN>/debug/routes | jq

2) Grep code for 'oauth2callback' and show the exact lines + surrounding 5 lines.
   grep -RIn --line-number --include='*.ts' --include='*.js' 'oauth2callback' server || true

3) Confirm the **middleware order**:
   - session() before passport.initialize()/passport.session()
   - body parsers (urlencoded) before /oauth2callback (for form_post)
   - passport.authenticate('replitauth', ...) registered for **GET** and **POST** on /oauth2callback
   - Static/SPA catch-all (e.g., app.use(express.static(...)) or app.get('*', ...)) comes **after** auth routes
   Print the relevant lines with numbers.

Phase 3 — Check callback method and body parsing

Replit OIDC can return with response_mode=form_post (POST). If you only handle GET, 404. If you don’t have urlencoded parser before the route, 404/400 can occur.

CallbackMethodProbe:
1) Hit both methods manually:
   curl -i "https://<REPLIT_DOMAIN>/oauth2callback" -H "X-Forwarded-Host: <REPLIT_DOMAIN>" | head -n1
   curl -i -X POST "https://<REPLIT_DOMAIN>/oauth2callback" \
     -H "X-Forwarded-Host: <REPLIT_DOMAIN>" \
     -H "Content-Type: application/x-www-form-urlencoded" \
     --data "code=fake&state=fake" | head -n1

2) Print where body parsers are mounted:
   - app.use(express.urlencoded({ extended: false }))
   - app.use(express.json())
   Confirm they appear **before** the callback routes.

3) Show the Strategy options: do we specify response_mode or use default? Print it.


Expected fix if broken: add

app.use(express.urlencoded({ extended: false })); // before routes
app.get('/oauth2callback', passport.authenticate('replitauth', opts));
app.post('/oauth2callback', passport.authenticate('replitauth', opts));